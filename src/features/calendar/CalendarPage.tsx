import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export function CalendarPage() {
  return (
    <PlaceholderPage
      title="Calendar"
      phase="Phase 2"
      description="Schedule and reschedule visits across day, week, and month views."
    />
  );
}
